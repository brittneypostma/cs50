#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void) 
{
    int key = get_int("Pick a number key for cypher: ");
    for (int i = 0; i < key; i++) 
    {
        
    }
}

/*ask user what the key (number offset) will be
only accept numbers in one line
loop over the message 
offset the number the user entered
print the encoded message
*/